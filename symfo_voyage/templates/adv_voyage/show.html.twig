{% extends 'base.html.twig' %}

{% block title %}AdvVoyage{% endblock %}

{% block body %}

    <h1>AdvVoyage </h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adv_voyage.id }}</td>
            </tr>
            <tr>
                <th>Destination_voyage</th>
                <td>{{ adv_voyage.destinationVoyage }}</td>
            </tr>
            <tr>
                <th>Duree_voyage</th>
                <td>{{ adv_voyage.dureeVoyage }}</td>
            </tr>
            <tr>
                <th>Image_voyage</th>
                <td>{{ adv_voyage.imageVoyage }}</td>
            </tr>
            <tr>
                <th>Description_voyage</th>
                <td>{{ adv_voyage.descriptionVoyage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voyage_index') }}">back to list</a>

    <a href="{{ path('app_voyage_edit', {'id': adv_voyage.id}) }}">edit</a>

    {{ include('adv_voyage/_delete_form.html.twig') }}




{% endblock %}
