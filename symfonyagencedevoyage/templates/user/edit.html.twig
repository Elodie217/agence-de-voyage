{% extends 'base.html.twig' %}

{% block title %}Nouvel utilisateur{% endblock %}

{% block body %}
    {# <h1>Create new User</h1>

    {{ include('user/_form.html.twig') }}

    <a href="{{ path('app_user_index') }}">back to list</a> #}


    {% include('includes/tableauDeBord.html.twig') %}

{# 082A33 #}

    <div class='flex justify-between items-center'>
        <h1 class=' m-6 text-3xl font-bold'>Créer un nouvel utilisateur</h1>
        <a href="{{ path('app_user_index') }}" class='block text-white py-1.5 px-4 mr-2 h-fit rounded transition duration-200 bg-[#FF9029] hover:bg-[#FF7B00] text-lg'>Retour aux utilisateurs</a>
    </div>

{{ form_start(form) }}
<div class="flex items-center justify-center p-12">
    <div class="mx-auto w-full max-w-[550px]">
        
        <div class="-mx-3 flex flex-wrap">
            <div class="w-full px-3 sm:w-1/2">
            <div class="mb-5">
                <label class='mb-3 block text-base font-medium text-[#07074D]"'>                
                {{form_label(form.nom_utilisateur) }}
                </label>
                {{ form_row(form.nom_utilisateur, {'label': false}) }}
            </div>
            </div>
            <div class="w-full px-3 sm:w-1/2">
            <div class="mb-5">
                <label class='mb-3 block text-base font-medium text-[#07074D]"'>                
                {{form_label(form.prenom_utilisateur) }}
                </label>
                
                {{ form_row(form.prenom_utilisateur, {'label': false}) }}
            </div>
            </div>
        </div>

        <div class="mb-5">
            
            <label class='mb-3 block text-base font-medium text-[#07074D]"'>                
            {{form_label(form.email) }}
            </label>
                {{ form_row(form.email, {'label': false}) }}

        </div>

        <div class="-mx-3 flex flex-wrap">
            <div class="w-full px-3 sm:w-1/2">
                <div class="mb-5">
                    <label class='mb-3 block text-base font-medium text-[#07074D]"'>                
                    {{form_label(form.telephone_utilisateur) }}
                    </label>
                    
                    {{ form_row(form.telephone_utilisateur, {'label': false}) }}
                
                </div>
            </div>
            {% if is_granted('ROLE_ADMIN') %}
            <div class="w-full px-3 sm:w-1/3">
                <div class="mb-5">
                    <label class='mb-3 block text-base font-medium text-[#07074D]"'>                
                    {{form_label(form.roles) }}
                    </label>
                    
                    {{ form_row(form.roles, {'label': false}) }}
                
                </div>
            </div>
            {% endif %}
        </div>
        {% if is_granted('ROLE_ADMIN') %}
        <div class="mb-5">
            
            <label class='mb-3 block text-base font-medium text-[#07074D]"'>                
            {{form_label(form.password) }}
            </label>
                {{ form_row(form.password, {'label': false}) }}

        </div>
        {% endif %}
        <div class= 'flex justify-between'>
            <button class="block text-white py-1.5 px-4 mr-2 h-fit rounded transition duration-200 bg-[#FF9029] hover:bg-[#FF7B00] text-lg">{{ button_label|default('Enregistrer') }}</button>
            {{ form_end(form) }}
            {# {{ include('user/_delete_form.html.twig') }} #}

        </div>
    </div>
</div>


{% endblock %}
